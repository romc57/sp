---
name: onboard
description: Onboard to this linked project. Use when starting work on a new repo in WorkStation.
---

# Onboard

1. Read WorkStation `.cursor/skills/work-with-projects/SKILL.md`
2. For AWS deploys read WorkStation `.cursor/skills/aws-deploy/SKILL.md`
3. Read wrapper `valerie/projects/<name>/project.yaml` and `memory/`
4. Run tests with commands from `project.yaml → commands`
